# Environment Configuration Template
# This file shows all possible environment variables
# For specific environments, use:
# - env.local.example (for local development)
# - env.staging.example (for staging deployment)
# - env.production.example (for production deployment)

# Telegram Bot Configuration
BOT_TOKEN=your_telegram_bot_token_here

# Environment Configuration
NODE_ENV=development  # development | staging | production

# Database Configuration
# Local: Uses PGlite (no DATABASE_URL needed)
# Staging/Production: Uses Supabase PostgreSQL
STAGING_DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[STAGING-REF].supabase.co:5432/postgres
PRODUCTION_DATABASE_URL=postgresql://postgres:[PASSWORD]@db.[PRODUCTION-REF].supabase.co:5432/postgres

# Admin Configuration
ADMIN_SECRET=your_admin_secret_password

# Optional: Channel IDs for broadcasts
VOLUNTEER_CHANNEL_ID=your_volunteer_channel_id
VOLUNTEER_TOPIC_ID=your_volunteer_topic_id

# Optional: Admin Channel ID for reports
ADMIN_CHANNEL_ID=your_admin_channel_id
ADMIN_TOPIC_ID=your_admin_topic_id

# Optional: For webhook deployment
WEBHOOK_URL=your_app_URL/api/webhook
